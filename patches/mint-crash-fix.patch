diff --git a/mint_lib/src/lib.rs b/mint_lib/src/lib.rs
index ee0d1b9..da73981 100644
--- a/mint_lib/src/lib.rs
+++ b/mint_lib/src/lib.rs
@@ -16,7 +16,7 @@ pub mod built_info {
     include!(concat!(env!("OUT_DIR"), "/built.rs"));
 
     pub fn version() -> &'static str {
-        GIT_VERSION.unwrap()
+        GIT_VERSION.unwrap_or(env!("CARGO_PKG_VERSION"))
     }
 }
 
diff --git a/src/gui/mod.rs b/src/gui/mod.rs
index 264870b..7ca7027 100644
--- a/src/gui/mod.rs
+++ b/src/gui/mod.rs
@@ -62,7 +62,7 @@ pub fn gui(dirs: Dirs, args: Option<Vec<String>>) -> Result<(), MintError> {
         ..Default::default()
     };
     eframe::run_native(
-        &format!("mint {}", mint_lib::built_info::GIT_VERSION.unwrap()),
+        &format!("mint {}", mint_lib::built_info::version()),
         options,
         Box::new(|cc| Ok(Box::new(App::new(cc, dirs, args)?))),
     )
diff --git a/src/main.rs b/src/main.rs
index 637fe3e..d6f1097 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -84,7 +84,7 @@ enum Action {
 }
 
 #[derive(Parser, Debug)]
-#[command(author, version=mint_lib::built_info::GIT_VERSION.unwrap())]
+#[command(author, version=mint_lib::built_info::version())]
 struct Args {
     #[command(subcommand)]
     action: Option<Action>,
