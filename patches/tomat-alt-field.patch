diff --git a/src/timer.rs b/src/timer.rs
index 3a182d5..e15ea0a 100644
--- a/src/timer.rs
+++ b/src/timer.rs
@@ -147,6 +147,7 @@ pub enum StatusOutput {
         tooltip: String,
         class: String,
         percentage: f64,
+        alt: String,
     },
     I3statusRs {
         text: String,
@@ -170,6 +171,7 @@ impl StatusOutput {
                 tooltip: MESSAGE.to_string(),
                 class: "disconnected".to_string(),
                 percentage: 0.0,
+                alt: "".to_string(),
             },
             Format::I3statusRs => Self::I3statusRs {
                 text: MESSAGE.to_string(),
@@ -687,6 +689,7 @@ impl TimerState {
                 tooltip,
                 class: class.to_string(),
                 percentage,
+                alt: class.to_string(),
             },
             Format::I3statusRs => {
                 // Map timer states to i3status-rs states
@@ -949,6 +952,7 @@ mod tests {
                 class,
                 tooltip,
                 percentage,
+                alt,
             } => {
                 assert_eq!(text, "🍅 25:00 ⏹");
                 assert_eq!(class, "idle");
@@ -978,6 +982,7 @@ mod tests {
                 class,
                 tooltip,
                 percentage,
+                alt,
             } => {
                 assert!(text.starts_with("🍅"));
                 assert!(text.ends_with("▶"));
